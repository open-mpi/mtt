"""
Complete actions needed for the REST server setup that require the newly
configured virtual environment.

This script is intended to be called solely by ../mtt_server_install.py.
"""

import os
import sys
import shutil
import sqlite3
from subprocess import call

# Nothing to do at the moment
